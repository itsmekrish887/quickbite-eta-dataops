Refine my existing QuickBite Operations Dashboard to make it cleaner and easier to present for a Senior AI Data Operations Manager case study.

IMPORTANT: Do not rebuild the app. Do not change the approved order schema, synthetic datasets, calculations, routes, upload functionality, filters, issue-detection logic, or other working business logic. Focus on simplifying the existing UI, removing duplicate information, and improving visual hierarchy.

The dashboard should tell four simple stories:

Overview = Is the system healthy?
Data = Can I trust the data?
Metrics = Where is the model failing?
Issues = What should Data Ops do about it?

OVERVIEW PAGE:

Simplify the page into four main sections.

1. Model Health: Keep MAE, P90 Error, Within ±10 min, and Underprediction Rate.

2. Operational Health: Show Data Readiness, Open P0 Issues, Active Vendor Batches, and Annotation QA as compact KPIs.

3. Needs Attention: Show only the top 3 highest-priority issues such as Mumbai × Heavy Rain, Bengaluru × Rider Reassignment, and New Restaurants. Show severity, cohort, key degradation metric, and a View Issue action.

4. Vendor & Batch Status: Keep the existing Batch Intake / Vendor Batch functionality but make it compact. Show Batch ID, Vendor, Progress %, QA Score, SLA, and Status.

Remove or heavily simplify the existing Operating Posture / Signal Board because it duplicates information available elsewhere.

DATA PAGE:

Keep the existing 6-step governed ingestion workflow:
Upload → Schema Validation → Data Quality → Preview → Classification → Approve Ingestion.

Keep the existing Data Readiness, Annotation QA, human approval, validation logic, and upload functionality.

Rename “Manual Edge-Case Entry” to “Proactive Edge Case Intake”.

Add this description:
“Log known upcoming operational risks before they appear as production model failures.”

This should support proactive risks such as new city launches, festivals, severe weather, operational changes, and new restaurant cohorts.

Do not change the approved order schema.

METRICS PAGE:

Keep this page focused only on model analysis.

Structure it as:
Global Performance → City Performance → Cohort Drill-Down.

Keep MAE, P90, Within ±10 min, Underprediction, city comparisons, existing filters, and drill-down functionality.

Do not add vendor operations, annotation QA, edge-case intake, or workflow management to this page.

The purpose of the Metrics page should be immediately clear: “Where is the model failing?”

ISSUES PAGE:

Merge the existing “Emerging Issues & Action Queue” and “Edge Case Radar” into one unified “Issues & Edge Cases” section.

Do not display the same incident twice.

Each issue should follow this structure:
Signal → Evidence → Root-Cause Hypothesis → Recommended Data Action → Human Decision.

For each issue, show the most relevant information from the existing system such as severity (P0/P1/P2), cohort, MAE, baseline MAE, P90, underprediction, affected records, degradation, suspected cause, recommended action, and human review/approval status.

Keep Targeted Data Acquisition, but simplify the visible name to “Targeted Data Plan”.

For a selected issue, show:
Cohort
Data Role: Baseline / Seasonal / Edge Case
Current Coverage
Desired Coverage
Coverage Gap
Sampling Strategy
Annotation Priority
Reason for Collection

The principle should be:
“Do not collect more data. Collect the data required to close a demonstrated model gap.”

TERMINOLOGY:

Reduce terminology overload across the application.

Prefer these labels:
Model Health
Data Health
Vendor Batches
City Performance
Issues & Edge Cases
Targeted Data Plan
Proactive Edge Case Intake

Do not create additional names for concepts that already exist.

DESIGN:

Keep the current professional visual identity, but make the dashboard cleaner and less crowded.

Use fewer cards, more whitespace, consistent spacing, consistent card heights, clear section hierarchy, and shorter labels.

Avoid large blocks of explanatory text inside the dashboard.

Do not add new sections unless absolutely necessary.

FINAL WORKFLOW:

The application should communicate this operating model:

Production → Monitor → Detect → Triage → Target Data → Vendor/Human Review → QA → Evaluate → Retrain → Regression Test → Human Approval → Production.

The application should feel like one connected AI Data Operations Control Tower rather than several unrelated dashboards.

AI can detect, triage, analyze, and recommend. Humans retain approval over consequential actions.

Make these changes using the existing components and architecture wherever possible. Do not unnecessarily rewrite working code.

After implementation, verify that Overview, Data, Metrics, and Issues still work and that uploads, calculations, filters, cohort drill-downs, issue detection, human decisions, and vendor batch functionality have not been broken.
